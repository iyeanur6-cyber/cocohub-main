/**
 * Shared constants for session monitoring.
 * Imported by both analyticsService and the frontend sessionMonitoringService.
 */

/** Crash-free session rate threshold — alerts fire when rate drops below this */
export const CRASH_FREE_THRESHOLD = 99.5;
